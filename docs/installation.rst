============
Installation
============

At the command line::

    $ easy_install stratified_bayesian_blocks

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv stratified_bayesian_blocks
    $ pip install stratified_bayesian_blocks
